
  <%= form_with(model: to_do) do |form| %>
    <div class=" p-2 border-bottom">
      <%= form.text_field :name, class: 'form-title border-0 fw-bold fs-3 text-black px-2', placeholder: 'Create To Do' %>
    </div>
    
    <div class="p-3 ">
      <%= form.submit "Add To Do", class: 'btn btn-secondary' %>
    </div>
<% end %>
